---
route: /components/card-header
menu: Utility Components
---

import { Playground, Props } from 'docz';
import { Card } from './Card.tsx';
import { CardHeader } from './CardHeader.tsx';
import { CardBody } from './CardBody.tsx';

# CardHeader

> This component is not exposed as `mml` string, it is used internally in other mml components and it can be imported to create your custom ones.

Helps display content in a card. Typically used with [`Card`](/components/card) and [`CardBody`](/components/card-body).

## Properties

<Props of={CardHeader} />

## Basic usage

Refer to the [`Card` docs](/components/card).
