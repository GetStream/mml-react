---
route: /components/success
menu: Utility Components
---

import { Playground, Props } from 'docz';
import { Success } from './Success.tsx';

# Success

> This component is not exposed as `mml` string, it is used internally in other Components and it can be imported to create your custom ones.

## Properties

<Props of={Success} />

## Basic usage

<Playground>
  <Success success="Request successfull, thanks" />
</Playground>
